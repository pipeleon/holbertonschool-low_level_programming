#include "main.h"
/* Task 6 0x04. C - More functions, more nested loops  */

/**
 * print_line - Prints lines
 * @n: number of lines
 * Description: Prints lines
 * Return: void
 */
void print_line(int n)
{
	int i;

	if (n > 0)
	{
		for (i = 1; i <= n; i++)
		_putchar(95);
	}
	_putchar('\n');
}
