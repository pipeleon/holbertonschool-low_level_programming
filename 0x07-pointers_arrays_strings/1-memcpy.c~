#include "main.h"
/* Task 0 0x07. C - Even more pointers, arrays and strings  */

/**
 * _memset - Fills memory with a constant byte
 * @s: Destiny of the filling
 * @b: Constant byte
 * @n: How many space to fill
 * Description: Fills memory with a constant byte
 * Return: char
 */
char *_memset(char *s, char b, unsigned int n)
{
	unsigned int i;

	for (i = 0; i < n; i++)
		s[i] = b;

	return (s);
}
