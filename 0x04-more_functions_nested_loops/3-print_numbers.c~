#include "main.h"
/* Task 2 0x04. C - More functions, more nested loops  */

/**
 * mul - Multiplies two integers
 * @a: Parameter to multiplie
 * @b: Parameter to multiplie
 * Description: Multiplies a and b
 * Return: int
 */
int mul(int a, int b)
{
	int c;

	c = (a * b);

	return (c);
}
